name: 
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Update MeiliSearch Image
      run: |
        export MEILI_IMAGE_VERSION=${{ vars.MEILI_IMAGE_VERSION }}
        envsubst < ./base/statefulset.yaml > ./base/statefulset_updated.yaml
        mv ./base/statefulset_updated.yaml ./base/statefulset.yaml

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.11'

    - name: Install Ansible
      run: |
        python -m pip install --upgrade pip
        pip install ansible

    - name: Run Ansible Playbook
      env:
        PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        USERNAME: ${{ secrets.SSH_USER }}
        HOST: ${{ secrets.SSH_HOST }}
      run: |
        echo "$PRIVATE_KEY" > private_key
        chmod 600 private_key
        ansible-playbook -i "$HOST," -e "ansible_ssh_common_args='-o StrictHostKeyChecking=no'" -e "ansible_ssh_private_key_file=private_key" playbook.yml